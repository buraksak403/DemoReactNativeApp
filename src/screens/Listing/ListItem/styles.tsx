import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    deleteButton: { fontSize: 20, position: 'absolute', top: 30, right: 30, zIndex: 100 },
    nameAgeView: { alignSelf: 'center', position: 'absolute', bottom: 10, backgroundColor: '#000000', padding: 6, borderRadius: 6 }
})

export default styles